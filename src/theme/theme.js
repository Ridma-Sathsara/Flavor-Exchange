import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'light', 
    primary: {
      main: '#509E2F',
    },
    secondary: {
      main: '#dc004e',
    },
  },
});

export default theme;